# 관리자 키 (미설정 시 기본값 weflow2026)
ADMIN_KEY=change-me

# Upstash Redis (Vercel Marketplace > Upstash 통합 추가 시 자동 주입됨)
# 로컬에서 Redis를 쓰려면 아래 두 값을 채우세요. 비워두면 data/submissions.json 파일에 저장됩니다.
KV_REST_API_URL=
KV_REST_API_TOKEN=
